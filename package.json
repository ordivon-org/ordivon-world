{
  "name": "@ordivon/edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26",
    "pnpm": ">=10"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/**/*.test.ts",
    "check:python": "python3 -m py_compile scripts/ordivon_edge_client.py scripts/ordivon_edge_release.py scripts/ordivon_edge_gc.py && python3 -m unittest discover -s test -p '*_test.py'",
    "check:operations": "scripts/check-operations",
    "check:boundary": "node scripts/check-boundary.mjs",
    "build": "node scripts/wrangler-dry-run.mjs",
    "ci": "pnpm typecheck && pnpm test && pnpm check:python && pnpm check:operations && pnpm check:boundary && pnpm build"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260727.1",
    "@types/node": "26.1.1",
    "tsx": "4.23.1",
    "typescript": "7.0.2",
    "wrangler": "4.114.0"
  }
}
