---
schema_version: 1
id: world.hp0-hp4-survival
title: High-Pressure Survival HP0–HP4
type: decision
profile: engineering
lifecycle: active
source_role: canonical
visibility: public
owners:
  - ordivon-world
audience:
  - maintainer
  - builder
  - agent
summary: Destructive survival audit that contracts World public surface, preserves consequence continuity, rejects premature wire genericization, and scopes Agent-facing authority language from fresh-model evidence.
evidence_status: verified
readiness: READY
applies_to:
  - ordivon-world
---
# High-Pressure Survival HP0–HP4

> **Subsequent product-surface update:** this document records the HP0–HP4 decision at its closeout. HP8 later removed the executable `ForeignEgressCapability` / `EffectPathQuery` modules and their three packaged contracts after no independent product consumer or fresh-Agent decision advantage emerged. Current authority is [`high-pressure-survival-hp6-hp8.md`](high-pressure-survival-hp6-hp8.md).

This round treated every existing World surface as removable. A completed P-stage, a unit test, or a document reference was not accepted as proof of current necessity. The allowed verdicts were retain, privatize, move owner, demote to research, archive, delete, or need more evidence.

## HP0 — Surface Court

The default Python facade contracted from 90 exported names to 17. Explicit pre-1.0 attributes remain importable for compatibility, but wildcard/default surface now emphasizes the live provider seam, three production trajectories, bounded Task inspection, temporal observation, contracts and telemetry. At the HP4 closeout, `EffectPathQuery`, `EffectPathCandidate` and `ForeignEgressCapability` were still packaged as explicit research/informational APIs rather than default facade members; HP8 later removed them from the product package.

The unused reserved `network-observation` Schema was deleted. No current producer plus independent consumer existed; owner-local network reports remain operational evidence. Five W5-A/W5-B falsifier scripts with no active caller were also removed; their accepted evidence, canonical research documents and Git history remain.

## HP1 — Owner Drift

World's pinned Host revision was challenged against Host `6495822162c69179e8ad4f8a0d79cc42902ff599`. Forty-four core recovery/trajectory tests passed unchanged, so the old revision is not a semantic replay requirement. However, refreshing the lock expanded the World Python graph from 9 to 34 packages because the Host distribution carries MCP/server dependencies. The candidate upgrade was therefore rejected for this round: World keeps the old exact pin temporarily while Host owns the packaging question.

Both old `ordivon-vpn` and Workstation `surfpath` are physically live. `surfpath` owns current multi-protocol discovery/revalidation; `ordivon-vpn` still owns isolated WireGuard namespace and key/profile invariants. World will not delete the latter until Workstation proves substitution, but physical network ownership should converge there rather than remain shared indefinitely.

## HP2 — Trajectory Kill Test

A direct Host relay bypassing World was tested for Resource, Message and Entity consequences. With only a retained `prepared` marker, an injected response loss after external commit caused every fresh controller to redispatch: all three families produced two external commits. Adding `UNKNOWN` plus exact-original reconciliation reduced every family to one external commit with no blind retry.

The survivor is therefore the consequence-continuity law, not a generic interaction class:

```text
Prepared → possible external consequence → UNKNOWN
                              ↓
                 reconcile exact original identity
                              ↓
                    only then consider retry
```

Resource, Message and Entity still keep different public semantics.

## HP3 — Mechanical Deduplication Tournament

Resource and Message wire files were intentionally genericized behind one private codec. The shadow candidate passed all 15 focused tests and Ruff, proving the mechanics can be shared. It nevertheless grew the combined implementation from 364 to 377 lines and added an indirection/parameterization layer. The expansion was reverted.

The retained law is: **structural similarity is not sufficient evidence for abstraction**. At this scale, local duplication is cheaper and clearer.

## HP4 — Fresh-Agent Projection A/B

Thirty fresh independent DeepSeek v4 flash evaluation Runs were executed through the owner-native Ordivon Harness, plus one Provider smoke. Raw versus projected evidence was tested for path choice, controller recovery and temporal availability.

Path projection did not earn production promotion. Neither raw evidence nor the first `EffectPathQuery` presentation reliably produced the prespecified Cloudflare-owner-revalidation choice, and the projected form produced inconsistent interpretations including one direct-ACT choice despite absent action authority. At that stage the implementation was retained as research evidence outside the default facade. HP8 later preserved the evidence but removed the executable implementation after no real consumer appeared.

Recovery produced a useful defect. Raw evidence already yielded the correct reconcile choice, while the first Inspector wording could make `reconcile-original-transfer` sound like re-issuing the request. World narrowed the projection to `actionAuthority=not-granted-by-inspection` and `reconcile-original-...-without-redispatch`. After that change, three of three fresh Runs completed with `RECONCILE_RESOURCE_ONLY` and explicitly described reconciliation as lookup/inspection rather than redispatch.

Temporal evidence produced the same naming lesson. `availableAt` remains necessary as the only World-owned coordinate for when the complete observation first became available; provider completion and Host admission are different owner clocks. The generic `authority` field caused fresh Agents to treat the timestamp itself as non-authoritative even after computing the correct 1.128-second interval. Renaming it `actionAuthority` removed that semantic collision: all clarity-run rationales recognized the temporal predicate, and choice-only trials produced the intended positive `USE_FOR_LATENCY_ONLY` result in two of three projected Runs. Model structured-output consistency remained noisy, so HP4 does not promote a broader temporal ontology.

## Surviving boundary

After HP0–HP4, the strongest World production core remains:

- language-neutral Resource / Message / Entity cross-owner contracts;
- owner-bound external-effect preparation, UNKNOWN and exact reconciliation;
- the Cloudflare Host-facing effect/recovery seam;
- bounded `WorldTaskInspector` with explicitly scoped `actionAuthority`;
- narrow `WorldObservation.availableAt` temporal provenance;
- owner-native facts and explicit Agent selection rather than a global manager.

The following are intentionally weaker than before the round:

- path/capability projections are explicit research/informational surfaces, not default facade;
- the reserved generic network-observation contract is gone;
- old W5 experimental runners are no longer active product apparatus;
- current Host compatibility is proven, but dependency packaging blocks a cost-free pin advance;
- network-tool ownership remains a Workstation migration question.

The next pressure stage should start at HP5 Provider/Verification Court, not by rebuilding the deleted or demoted surface.
