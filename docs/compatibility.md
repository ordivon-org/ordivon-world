# Compatibility

## Supported graph

| Component | Supported value |
|---|---|
| Python | `>=3.12,<3.13` |
| Ordivon Host | `ebaf6ef90d87e7bc524e8f30d71521b371d17f2e` |
| Ordivon Protocol | `420dc356cb664d75db0f34f356156baebe5843db` through Host |
| Node.js | `>=26` for the Cloudflare provider |
| pnpm | `>=10,<11`, lock generated by `10.33.2` |
| Cloudflare provider contract | Fetch v2, Browser Snapshot v2, Receipt v2, Artifact Get v1 |
| JSON Schema | Draft 2020-12 |

`uv.lock` and `pnpm-lock.yaml` are required release inputs. Compatibility claims apply only to the locked graph.

HP1 survival testing established compatibility with newer Host behavior before the package pin advanced. Host has since separated its core distribution from the optional MCP server dependency, so World now consumes the current narrow Host core pin without inheriting the server stack. Historical acceptance evidence retains the exact older revisions it tested; the supported graph above follows the current locked package authority.

## Pre-1.0 policy

The package is experimental. Public Python names exported from `ordivon_world` are the recommended facade, but breaking changes may occur before 1.0 when accompanied by:

- a documented failure or requirement;
- a migration note;
- updated contract and live acceptance evidence;
- no reinterpretation of retained provider Receipts.

The 0.5 line uses that policy to retire the pre-0.5 generic Resource planning API. Code importing `ordivon_world.resource_discovery`, `ResourceOpportunityBoard`, `AcquisitionAssessment`, `evaluate_resource`, or related ranking helpers must migrate to the consuming owner/domain. The R0–R10 semantic doctrine remains available as research; no retained external Dispatch/Receipt/UNKNOWN identity is reinterpreted.

The 0.6 line additionally retires World-owned controller-replacement verdicts and transparent pre-multi-instance flat Host-state upgrades. `WorldTaskInspector.inspect_task()` remains the owner-native commitment/current-recovery projection; controller/morphology replacement policy belongs to Harness/Host/the consuming controller. Pre-P4 Resource, pre-M5 Message and pre-P5 provider flat namespace state must be reconciled/upgraded with a pre-0.6 World client before moving to 0.6. Current Host authority was checked before this retirement and contained zero `world` extension namespace rows, so no current durable World relation required the old automatic migration path. Current v5 Host namespace state, v1 Resource/Message/Entity contracts, provider request identity, Receipt/UNKNOWN semantics and the frozen pre-HP5 TraceContext decoder remain supported.

## Durable compatibility

The most important compatibility surface is not import syntax. It is the ability to reopen retained state and reconcile the same external action.

Changes must preserve or explicitly migrate:

- `PreparedWorldDispatch` fields and digest semantics;
- deterministic provider request identity;
- provider request digest verification;
- Host Dispatch and Observation identities;
- Cloudflare Receipt and Artifact references;
- UNKNOWN versus failed/rejected distinctions;
- capability-condition invalidation behavior.

## Provider compatibility

Provider-native schemas remain local to the provider. A future second provider may implement the same high-level adapter operations without sharing Cloudflare lease, R2, policy or error fields. Do not normalize provider facts that are not semantically interchangeable.

## Deployment compatibility

Cloudflare Worker identity is based on deployable Worker inputs, not the whole repository commit. A Host adapter, documentation, test or GC-controller-only change can update the repository without requiring Worker redeployment when the Worker-input digest remains unchanged.

Installed operator controllers must match source digests before live health is considered current.
