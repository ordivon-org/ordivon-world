# WDF1 Closeout — Nomology / Modality / Control / Probability Foundations

Status: **WDF1 complete and frozen at the research level.** WDF0 Meta-Foundation v1 remains frozen; no WDF0 reopen condition fired. No production engineering refactor is admitted by WDF1 closeout.

## 1. Canonical numbering rule

From this closeout onward, World Deep Foundations uses exactly one canonical round number after `WDF` and, when a major round has multiple sub-rounds, exactly one letter suffix:

```text
WDF<number>-<letter>
```

Examples:

```text
WDF1-A
WDF1-B
...
WDF1-G
WDF2-A
```

Do **not** add an extra numeric nesting layer between the major WDF number and its letter suffix.

Internal checklist items inside one document may use ordinary local numbering, but they do not become canonical round IDs and should not be used for cross-conversation continuation.

Historical Task IDs / Git commits are not rewritten; only the research-round nomenclature is normalized.

## 2. Historical normalization

The pre-closeout nested numbering has been flattened, in research order, into the canonical sequence:

```text
WDF1-A → WDF1-B → WDF1-C → WDF1-D → WDF1-E → WDF1-F → WDF1-G
```

The next major round begins at:

```text
WDF2-A
```

Git rename/history preserves pre-normalization provenance; canonical research documents do not propagate the legacy nested labels.

## 3. Canonical WDF1 sequence

### WDF1-A — Nomology / Modality / Causality Current-Use & Hidden-Assumption Inventory

Established the initial term separation, rival law/causal families and dependency/circularity map.

Canonical artifact:

`WDF1-A-NOMOLOGY-MODALITY-CAUSALITY-INVENTORY.md`

### WDF1-B — Matched Modal Separation Cases

Used matched cases to separate nomological possibility, actual-state reachability, Actor control, feasibility, access, authorization, epistemic openness and probability/chance roles.

Canonical artifact:

`WDF1-B-MATCHED-MODAL-SEPARATION-CASES.md`

### WDF1-C — Candidate Modal Architectures

Compared nested filters, vector profiles, parameterized reachability and typed pluralism; reconstructed TARA as a provisional shared meta-architecture.

Canonical artifact:

`WDF1-C-CANDIDATE-MODAL-ARCHITECTURES.md`

### WDF1-D — Law / Initial Condition / Boundary Condition / Constraint Separation

Established `MathematicalRole != NomologicalStatus`; rejected law = update-rule / history-filter / generic-constraint reductions and isolated a theory-relative NomologicalStatus interface.

Canonical artifact:

`WDF1-D-LAW-INITIAL-BOUNDARY-CONSTRAINT-SEPARATION.md`

### WDF1-E — Dynamic Reachability / Control / Intervention Architecture

Separated natural reachability, passive/mediated attainment, open-loop/feedback/robust/strategic control, viability/recovery and intervention. Reconstructed Capability as a scoped supported projection rather than a root primitive.

Canonical artifact:

`WDF1-E-DYNAMIC-REACHABILITY-CONTROL-INTERVENTION-ARCHITECTURE.md`

### WDF1-F — Objective Modality / Epistemic Openness / Probability / Chance Separation

Separated formal probability measure from interpretation/truth role; rejected Probability and Uncertainty as roots; retained ObjectiveChance as unresolved Reality-facing interface.

Canonical artifact:

`WDF1-F-OBJECTIVE-MODALITY-EPISTEMIC-PROBABILITY-CHANCE-SEPARATION.md`

### WDF1-G — Modal Architecture Reconstruction / Falsification / Closeout

Performed destructive compression. TARA was rejected as Reality ontology but survived/froze as a Typed Modal Claim Grammar (TMCG). Ranked the next residuals and admitted WDF2.

Canonical artifact:

`WDF1-G-MODAL-ARCHITECTURE-RECONSTRUCTION-CLOSEOUT.md`

## 4. Frozen WDF1 Modal Foundation v1

The positive core is intentionally thin:

```text
Modal claim
= target/proposition/outcome
+ typed modal truth role
+ alternative/contrast specification or generator
+ typed relation/generator/evaluator
+ typed operator/quantifier/measure
+ explicit dependence/background
+ anchor when relevant
+ measure interpretation when relevant
+ model/evidence provenance when relevant.
```

This is a **Typed Modal Claim Grammar**, not a claim that Reality is made of TMCG components.

Core frozen firewalls include:

```text
Actual != Necessary
NomologicallyPossible != ReachableFromActuality
NaturalReachability != ActorControl
Reachability != Viability/Safety
Access != Authorization
PhysicalControlAbility != AuthorizedControlAbility
CapabilityToObtain != CapabilityToProduce
MathematicalRole != NomologicalStatus
ConstraintRole != OneConstraintOntology
ProbabilityMeasure != ProbabilityInterpretation
ProbabilityZero != Impossibility
ProbabilityOne != ModalNecessity
ModalSupport != ProbabilityWeight
Credence != ObjectiveChance
EpistemicUncertainty != ObjectiveStochasticity
ActionOccurrence != Intervention
Control != Intervention
ModelModalProjection != RealityModalTruth
CurrentAlternativeDomain != CompleteRealityModalDomain
```

## 5. Root deletions / demotions

WDF1 does not retain these as universal World root primitives:

```text
untyped Possible
untyped Necessary
universal Reachability
Feasibility
Access
Capability
Constraint
Probability
Uncertainty
Intervention as model-free primitive
TARA as Reality ontology
```

They remain available as typed/scoped projections where useful.

## 6. Explicit open interfaces

WDF1 intentionally leaves open:

```text
NomologicalStatus / Law metaphysics
ObjectiveChance
Counterfactual semantics
Causal truth / causal plural architecture
Metaphysical modality if later required
Fundamental physical modal structure
Formal open-world alternative-domain expansion
```

## 7. Production disposition

Current production `ordivon-world` remains intentionally narrower:

```text
Host-owned semantics
↔ independently authoritative environments
Bind → Observe → Act → Reconcile
```

Current production is already broadly compatible with WDF1:

```text
historical != current
UNKNOWN remains epistemic/reconciliation state
route/reachability != authority
no global CapabilityManager
Action != Effect != Outcome != Attribution
owner-native current evidence/currentness
response loss -> reconcile original request
```

No `ModalManager`, `PossibilityRegistry`, `CapabilityOntologyService`, universal probability object or production TMCG schema is admitted.

```text
Research grammar != Production object model
```

## 8. Residual ranking

After WDF1:

```text
1. Counterfactual Architecture        CRITICAL
2. Causal Architecture                CRITICAL
3. Law Metaphysics                    CRITICAL, interface-isolated
4. Objective Chance                   HIGH/CRITICAL, operationally isolatable
5. Fundamental Physical Modality      HIGH / domain-specific
```

This is research-order ranking, not ontological priority.

## 9. Exact continuation

The next canonical round is:

# WDF2-A — Counterfactual Current-Use / Term Separation / Hidden-Assumption Inventory

WDF2 as a whole studies:

```text
Counterfactual Architecture
= Alteration
+ Invariance
+ Alternative Generation / Selection
+ Similarity / Minimal Change
+ Structural Surgery
+ Identity / Continuity under alteration
+ probabilistic counterfactual typing
```

WDF2-A must begin by separating at least:

```text
counterfactual
hypothetical
possibility
intervention
alternative
nearest / similar
minimal change
hold fixed / invariant
ceteris paribus
would / would-have
could
prevention / omission
rule change
mechanism change
```

No Lewis, Stalnaker, SCM/Pearl, interventionist, powers/dispositional or law-relative account is preselected.

## 10. New-conversation handoff

Canonical continuation instruction:

> 继续 Ordivon World Deep Foundations。读取 `task:world-deep-foundations-wdf2-20260818` 和 repo 中冻结的 WDF0、WDF1 foundations，从 **WDF2-A — Counterfactual Current-Use / Term Separation / Hidden-Assumption Inventory** 开始。不要重做 WDF0/WDF1；只有出现具体 FoundationReopenCondition 才重新打开。编号统一使用 `WDF<number>-<letter>`，不要再引入额外数字嵌套层级。

WDF1 is closed.
