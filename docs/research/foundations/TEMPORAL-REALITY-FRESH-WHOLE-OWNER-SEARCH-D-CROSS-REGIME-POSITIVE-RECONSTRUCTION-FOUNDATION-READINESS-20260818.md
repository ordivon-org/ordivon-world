# Fresh Temporal Reality Whole-Owner Search — Round D

## Cross-Regime Positive Reconstruction / Grounding-Family Separation / Foundation-Candidate Readiness Test

Date: 2026-08-18

Status: **D COMPLETE / DUAL CORE POSITIVELY RECONSTRUCTED ACROSS REGIMES / TEMPORAL-SPECIFIC OWNER CONTENT SURVIVES GENERIC-RELATION-MEASURE CONTROL / FOUNDATION-CANDIDATE STAGE EARNED / NO FOUNDATION FROZEN / NO WDF NUMBER / NO CANONICAL PROJECT NAME**

Canonical input:

```text
C1 = Temporal Relational–Topological Structure
C2 = Temporal Interval / Metric Structure
I1 = Frame / Reference / Realization Interface
I2 = Cross-Temporal-Structure Bridge Interface
```

Round D requires positive explanatory reuse across regimes rather than merely surviving deletion tests.

---

# 1. Minimal positive temporal claim grammar

D introduces a falsifiable research grammar, not a Foundation schema.

## C1 claims

```text
TemporalRelation_R(x,y | K,CVE,Omega)
```

where:

```text
x,y = typed relata accepted under target criterion K
R = temporal relation family admitted in temporal structural context Omega
CVE = validity envelope
Omega = declared temporal structural context/regime
```

Examples of R where admitted:

```text
earlier/later
precedes/follows
overlaps
coincides/simultaneous-under-frame
comparable/incomparable
branch-related
cyclically related
belongs-to-temporal-domain
```

## C2 claims

```text
TemporalInterval_Delta(x,y or path | K,CVE,Omega)
```

where Delta may denote, when the regime supports it:

```text
duration
proper-time-like interval
elapsed interval
period
measure/scale
path-dependent temporal separation
```

No universal scalar or metric is presupposed.

## Interfaces

```text
I1 Reference/Realization:
  map clock/frame/time-scale/coordinate representation
  to C1/C2 claims.

I2 Cross-Temporal-Structure Bridge:
  relate temporal structures Omega1 and Omega2
  under explicit WDF3/WDF5 bridge/transport conditions.
```

---

# 2. Why this is not generic relation + generic measure vocabulary

Control hypothesis:

```text
C1 = generic relation/topology
C2 = generic measure/metric
therefore Temporal owner is vacuous duplication.
```

D rejects this.

The temporal owner adds a specific truth-role constraint:

```text
C1/C2 concern temporal placement, succession, overlap, separation,
coincidence or extent of occurrences/states/processes/boundaries
within a declared temporal realization/context.
```

A generic relation such as:

```text
friend-of
is-subset-of
is-connected-to
is-cheaper-than
```

is not temporal merely because it is relational.

A generic metric such as:

```text
Euclidean spatial distance
semantic embedding distance
network hop distance
utility loss
```

is not temporal merely because it is quantitative.

Temporal use requires temporal-role semantics, including where applicable:

```text
succession/localization across realizations
interval/duration interpretation
compatibility with temporal topology
frame/path-sensitive temporal meaning
host/local/reference temporal bridges
```

Therefore:

```text
TemporalRelation != GenericRelation by identity
TemporalInterval != GenericMeasure by identity
```

C1/C2 instantiate generic structural forms but add owner-specific semantics, just as WDF5 uses generic relations/structures without becoming generic mathematics.

---

# 3. Newtonian/classical positive reconstruction

Canonical classical template:

```text
Omega_N = (T, <, metric/affine interval structure, reference conventions)
```

Typical C1:

```text
t1 < t2
simultaneity under global classical frame
ordered trajectory positions
```

Typical C2:

```text
Delta t = t2 - t1
period
frequency inverse relation
lifetime/duration
```

I1:

```text
calendar/clock coordinates realize temporal localization and intervals.
```

The architecture reproduces classical use without universalizing:

```text
global simultaneity
global scalar time
path independence
```

outside this regime.

Result:

```text
NewtonianFit = PASS
```

---

# 4. Special relativity positive reconstruction

Special relativity requires C1/C2 separation to remain explicit.

C1 may encode:

```text
timelike precedence / chronological relation
spacelike incomparability for invariant before/after claims
frame-relative simultaneity
null/timelike relation classes where physical specialization supplies them
```

C2 may encode:

```text
proper-time interval along timelike worldlines
coordinate-time interval under a selected frame
```

I1 carries frame/reference semantics.

Key positive result:

```text
same physical event pair
can have different coordinate-time representations
without changing the underlying admissible temporal/spacetime relation.
```

The generic grammar therefore supports:

```text
frame-relative temporal localization
+
path/worldline-relative metric structure
```

without identifying frame with target reality.

Result:

```text
SRFit = PASS
```

---

# 5. General relativity / CTC positive reconstruction

GR pressures the architecture further.

C1 must permit:

```text
local temporal/timelike relations
local/global topology distinction
cyclic temporal structures in CTC-containing solutions
absence of one universal global temporal order
```

C2 permits:

```text
proper-time-like intervals along worldlines
metric dependence on dynamical geometry
path dependence
```

The architecture does not attempt to own:

```text
metric tensor ontology
gravity
full causal cone geometry
spatial topology
```

which remain in the Spacetime/fundamental-physics route.

C1/C2 instead expose the temporal claims consumed from that physical realization.

Result:

```text
GRFit = PASS
```

with explicit future-owner boundary preserved.

---

# 6. Relational / emergent quantum-time reconstruction

A relational quantum-time realization may begin with:

```text
globally constrained/stationary state
subsystem chosen as clock/reference
correlations/conditional states
```

D maps this to:

```text
Grounding machinery
-> produces/realizes temporal structure

C1
-> conditional/relational ordering or correlation-as-temporal-relation claims

C2
-> clock-parameter/interval structure where defined

I1
-> identifies clock subsystem/reference semantics
```

Current research continues to show that relational clocks can yield effective dynamics without one preferred external classical time, and finite-resolution clocks can alter effective dynamical properties. citeturn625927academia25turn625927academia26

This confirms:

```text
ArchitectureQuestion != GroundingQuestion
```

and:

```text
C1/C2 can describe emergent temporal structure
without claiming Time is primitive.
```

Result:

```text
RelationalQuantumFit = PASS PROVISIONALLY
```

The exact quantum grounding ontology remains open.

---

# 7. Distributed/computational positive reconstruction

Distributed systems provide a nonphysical but precise temporal regime.

C1 may encode:

```text
happens-before
process-local succession
concurrency/incomparability
chosen serialization extensions
```

Lamport's classic construction explicitly separates the partial happens-before relation from logical timestamp machinery and algorithmic total ordering. citeturn625927search0

C2 may be:

```text
absent for generic happens-before pairs
or separately supplied by monotonic/wall-clock/latency structures.
```

I1 maps:

```text
logical clocks
wall clocks
monotonic clocks
```

to the relevant temporal targets.

This is a decisive positive case because:

```text
C1 can be nontrivial while C2 is partial/independent.
```

Result:

```text
DistributedFit = PASS
```

---

# 8. Institutional / constituted temporal reconstruction

Institutional regimes can constitute:

```text
effective dates
expiry
terms
filing periods
business days
retroactive/prospective status
```

D decomposes:

```text
WDF3
-> constitutes category/status/rule criterion

C1
-> temporal relation/order/interval-membership structure

C2
-> duration/term where defined

I1
-> calendar/time-reference realization

I2
-> bridge to physical/reference chronology
```

A law can assign effect to a past calendar interval without altering physical past events.

Thus the generic temporal grammar remains useful while grounding is constituted/institutional.

Result:

```text
InstitutionalFit = PASS
```

---

# 9. Synthetic / virtual positive reconstruction

Synthetic worlds may define:

```text
pause
rewind
reset
branch
turn-based advancement
local accelerated/slowed clocks
```

D treats each declared synthetic world as having its own Omega_s where C1/C2 claims are evaluated.

Host physical execution has Omega_h.

I2 explicitly handles:

```text
Bridge(Omega_h, Omega_s)
```

Examples:

```text
1 host second -> 60 simulation seconds
host execution continues while simulation time pauses
rewind changes synthetic temporal localization/history state
without reversing host physical chronology
```

This cannot be represented safely by one universal scalar time.

C1/C2 + I2 handle the case without a new temporal primitive.

Result:

```text
SyntheticFit = PASS
```

---

# 10. Biological / multiscale reconstruction

Biology uses several temporal structures simultaneously:

```text
organism age
developmental stage
circadian phase
cell cycle
generation time
evolutionary lineage/history
reaction times
```

D separates:

```text
C1
-> succession/stage/phase/order relations where temporal

C2
-> age/duration/period/generation interval

WDF3/domain biology
-> what entity/stage/lineage counts as same or constituted

I1
-> clock/oscillator/reference mapping
```

Different biological clocks may drift relative to one another while target processes continue.

No unique `biological time substance` is required.

Result:

```text
BiologicalFit = PASS
```

---

# 11. Agent-era reconstruction

Agent systems compose multiple temporal structures:

```text
reality event order
sensor observation order
message delivery order
provider processing intervals
commit/order history
policy-effective intervals
planning horizons
lease/authorization windows
simulation branches
```

D maps:

```text
C1
-> event/concurrency/precedence relations

C2
-> latency/duration/expiry/horizon intervals

I1
-> clock/reference source

I2
-> mappings across host/provider/simulation/institutional temporal structures
```

This supports generic temporal semantics while preserving engineering ownership elsewhere.

Result:

```text
AgentEraFit = PASS
```

---

# 12. Grounding-family separation

D distinguishes temporal architecture from how it is grounded/realized.

## G1 Fundamental/external grounding

Temporal structure is taken as primitive/background/fundamental in a theory.

C1/C2 express its temporal truths.

## G2 Spacetime-geometric grounding

Temporal structure is realized as an aspect of Lorentzian/dynamical spacetime geometry.

C1/C2 expose temporal content while full geometry remains external owner depth.

## G3 Relational grounding

Temporal relations are grounded in relations among changes/configurations/subsystems/clocks.

C1/C2 describe the resulting temporal structure.

## G4 Emergent/effective grounding

Temporal roles arise only at an effective/coarse-grained/configuration level.

C1/C2 remain applicable to the emergent level.

## G5 Constituted grounding

Institutional/synthetic rules constitute temporal statuses, calendars or local temporal advancement.

C1/C2 state resulting temporal relations/intervals under WDF3 criteria.

Canonical:

```text
GroundingFamily
!= TemporalClaimArchitecture
```

No grounding family is selected as universal.

---

# 13. Does C1/C2 duplicate WDF0 generic occurrence/process grammar?

No.

WDF0 supplies:

```text
occurrence/process/change/history distinctions
representation/reality firewalls
generic relation vocabulary
```

It does not positively state:

```text
which temporal relational/topological structures hold
which temporal intervals/measures are defined
how multiple temporal structures relate
```

Therefore:

```text
WDF0 substrate != Temporal owner architecture
```

---

# 14. Does C1 duplicate WDF3 identity/criterion/bridge grammar?

No.

WDF3 supplies:

```text
what counts as same target/entity/category
under which criterion/CVE
and generic bridge validity.
```

C1 supplies temporal target semantics:

```text
earlier/later/coincident/incomparable/branch/cyclic/local-domain relations.
```

WDF3 can type the relata and bridge without determining the temporal relation.

Therefore:

```text
Criterion/Bridge != TemporalRelation
```

---

# 15. Does C2 duplicate WDF5 Property Evaluation?

No.

WDF5 can validate a measurement claim:

```text
duration(x,y) = 5 s
```

but it does not establish what temporal interval structure means or where such intervals are defined.

Temporal C2 owns target temporal interval semantics.
WDF5 owns attribution/reference/quality of evaluating those intervals.

Therefore:

```text
TemporalIntervalTarget
!= EvaluationOfTemporalInterval
```

This is one of D's strongest owner-boundary results.

---

# 16. I1 sufficiency after positive reconstruction

I1 Frame / Reference / Realization is sufficient for the tested cases.

It handles:

```text
coordinate frames
proper-time clocks
logical clocks
calendar standards
biological oscillators
simulation clocks
```

without becoming owner core.

No tested case requires temporal reference to create the target relation/interval by itself.

Therefore:

```text
I1CorePromotion = NOT EARNED
```

---

# 17. I2 sufficiency after positive reconstruction

I2 Cross-Temporal-Structure Bridge is also sufficient as an interface.

Tested bridge classes:

```text
proper time -> TAI/TT/UTC
host physical time -> simulation time
calendar/institutional time -> physical/reference chronology
logical/monotonic/wall-clock mappings
multiple local clock structures
```

BIPM explicitly treats TAI as a continuous time scale produced from realizations of the SI second and describes relativistic transformation from a clock's proper time into TAI/TT-related reference structure. citeturn625927search2turn625927search7

No extra temporal-specific bridge primitive beyond:

```text
C1/C2 semantic target
+
WDF3 bridge/CVE
+
WDF5 transport/reference validity
```

is yet required.

Therefore:

```text
I2CorePromotion = NOT EARNED
```

---

# 18. Current metrology is a positive boundary case, not merely a falsifier

The current SI second is defined by fixing the numerical value of the Cs-133 hyperfine transition frequency; BIPM is actively pursuing a roadmap toward possible optical redefinition, with ratification anticipated no earlier than 2030. citeturn625927search4turn625927search3turn625927search5

This provides a positive composition:

```text
C2
= target interval/duration semantics

I1
= frequency/clock realization

WDF5
= reference, attribution, traceability, quality
```

The realization can improve/change while C2's owner role remains stable.

---

# 19. Positive cross-regime invariant

Across the tested regimes, the stable reusable pattern is not one universal `time substance`.

It is:

```text
A temporal regime may support:

1. a typed relational/topological structure C1;
2. an interval/metric structure C2 where defined;
3. reference/realization interfaces I1;
4. bridges I2 when multiple temporal structures coexist;
5. independent grounding stories for why the temporal structure exists.
```

Different regimes can instantiate only subsets/richer versions.

Thus:

```text
TemporalArchitecture = TYPED / PARTIAL / REGIME-RELATIVE
not one globally uniform structure.
```

---

# 20. Positive minimality test

Can the architecture be compressed further after positive reconstruction?

## Remove C1

Fails in distributed partial-order/concurrency cases and directed/local topology claims.

## Remove C2

Fails in proper-time/duration/period/lifetime/rate claims.

## Promote I1 to core

Unnecessary; target temporal truths survive reference changes and reference semantics are heavily WDF5-owned.

## Promote I2 to core

Unnecessary; bridge is specialization of existing bridge/transport architecture.

## Add arrow core

Unnecessary; directed relations live in C1 while preferred-arrow grounding remains external.

## Add grounding core

Invalid category error: grounding theory answers why/how C1/C2 arise, not the minimal temporal claim role.

Result:

```text
PositiveMinimality(C1,C2) = SURVIVES D
```

---

# 21. Foundation-candidate readiness test

A later Foundation-candidate stage is warranted only if D establishes:

```text
cross-regime positive reuse
owner-specific semantics
stable upstream boundaries
no hidden core
positive minimality
falsifier readiness
```

D result:

```text
CrossRegimePositiveReuse = PASS
OwnerSpecificSemantics = PASS
UpstreamBoundaryStability = PASS
HiddenCoreSearch = PASS SO FAR
PositiveMinimality = PASS PROVISIONALLY
FalsifierReadiness = PASS
```

Therefore:

```text
FoundationCandidateStageReadiness
= EARNED
```

But:

```text
FoundationFreezeReadiness
= NOT ESTABLISHED
```

---

# 22. Provisional Foundation-candidate claim family

This is **not** a Foundation and receives no number.

## FC1 — Temporal Relational–Topological Responsibility

Candidate:

```text
A temporal model/claim must distinguish the temporal relational/topological
structure actually licensed in its regime from stronger assumptions such as
universal total order, global acyclicity or global simultaneity.
```

## FC2 — Temporal Interval / Metric Responsibility

Candidate:

```text
Temporal interval/metric claims are separately licensed from relational/topological
claims and may be local, partial, frame/path dependent or absent.
```

## FC3 — Reference/Bridge Non-Identity Firewall

Candidate interface law:

```text
Clock/reference/coordinate/bridge realization
!= temporal target structure by identity.
```

FC3 is likely boundary/interface material rather than a third Foundation core.

---

# 23. Candidate claim-level falsifiers for later closeout

A later destructive-closeout stage must reopen/revise the candidate if any exact case establishes:

```text
F1 A valid generic temporal architecture can eliminate C1 entirely without
   silently importing temporal relational/topological structure.

F2 A valid generic temporal architecture can eliminate C2 entirely without
   silently importing temporal interval/measure information.

F3 A cross-regime temporal truth requires a third owner-native burden that cannot
   be represented by C1/C2 plus WDF0-WDF5 interfaces.

F4 A tested regime shows clocks/references constitute temporal target structure
   in a way irreducible to C1/C2 + WDF3/WDF5, forcing I1 into core.

F5 Cross-temporal-structure transport requires temporal-specific bridge primitives
   not reducible to WDF3 bridge grammar + WDF5 transport/reference semantics.

F6 C1/C2 prove to be semantically vacuous duplicates of generic relation/topology
   and metric/measure owners with no temporal-specific truth role.

F7 Fundamental/relational/emergent/constituted grounding families cannot share
   the same C1/C2 claim architecture without circularity or semantic distortion.
```

---

# 24. Naming pressure — noncanonical

D allows naming pressure but no selection.

Current candidates:

```text
N1 Temporal Reality Architecture
N2 Temporal Structure Architecture
N3 Temporal Relations & Intervals Architecture
N4 Temporal Relational-Metric Architecture
N5 Time / Temporal Structure Architecture
```

Pressure:

```text
N1 broad but potentially vague
N2 clean and theory-neutral
N3 precise but may overfit current dual core
N4 technically precise but awkward and prematurely structural
N5 useful continuity label but `Time` remains overloaded
```

Current best pressure:

```text
Temporal Structure Architecture
```

but:

```text
CanonicalTemporalProjectName = NOT SELECTED
```

A later naming tournament is required.

---

# 25. No-Foundation control after positive reconstruction

Re-run:

```text
WDF0/WDF3/WDF5 + local theories suffice.
```

D rejects it more strongly than C.

Reason:

The same positive temporal truth roles recur across:

```text
classical physics
relativity
relational quantum time
distributed computation
institutions
synthetic worlds
biology
Agent systems
```

while upstream foundations supply only relata criteria, bridge grammar and evaluation/reference semantics.

Therefore:

```text
NoFoundationControl
= REJECTED AT OWNER-LINE LEVEL AFTER POSITIVE RECONSTRUCTION
```

Again this earns a Foundation-candidate stage, not freeze.

---

# 26. Project-split control after positive reconstruction

Could C1 and C2 be separate owner projects?

D rejects split.

They repeatedly compose around the same temporal referent:

```text
SR/GR
clock/metrology
biological periods
institutional terms
synthetic time scales
Agent latency/expiry
```

and the central scientific challenge is often exactly the relation between them.

Therefore:

```text
ProjectSplit = NOT EARNED IN D
```

---

# 27. Architecture-grounding matrix

```text
                         C1        C2       I1/I2      Grounding

Newtonian/classical      YES       YES      YES         fundamental/background
SR                        YES       YES      YES         spacetime-geometric
GR/CTC                    YES       YES      YES         dynamical spacetime
Relational quantum        YES       optional/YES YES     relational/emergent
Distributed computing     YES       optional  YES        constituted/algorithmic
Institutional             YES       optional/YES YES     constituted
Synthetic/virtual         YES       optional/YES YES     constituted/host-bridged
Biological/multiscale     YES       YES      YES         physical/effective
Agent systems             YES       YES      YES         mixed/constituted/physical
```

This matrix is qualitative and noncanonical.

It shows C2 need not exist globally/equally in every temporal regime, while C1 also need not reduce to one order form.

---

# 28. External research pressure retained in D

External sources are used only as current falsifier/realization evidence.

BIPM establishes current second/time-scale realization and relativistic proper-time/reference-scale distinctions. citeturn625927search2turn625927search4

Lamport's original distributed-systems paper remains the canonical precise example of partial event order plus logical-clock construction. citeturn625927search0

Braun 2025 provides a modern `order and number` spacetime reconstruction result, supporting strong composition between relational structure and scale/measure without proving universal reduction. citeturn625927academia24

Recent relational-clock research continues to show temporal dynamics reconstructed relative to physical clocks, including periodic clocks and finite-resolution clock effects. citeturn625927academia25turn625927academia26

---

# 29. Reopen audit

```text
FoundationReopenCondition(WDF0) = NOT FIRED
FoundationReopenCondition(WDF1) = NOT FIRED
FoundationReopenCondition(WDF2) = NOT FIRED
ExactWDF3ClaimFalsified = NONE
FoundationReopenCondition(WDF4) = NOT FIRED
FoundationReopenCondition(WDF5) = NOT FIRED
```

D depends on their existing boundaries and finds no exact counterexample.

---

# 30. Canonical Round D results

```text
D-1 Minimal positive C1/C2 claim grammar constructed.
D-2 Temporal-specific semantics survive generic relation/measure duplication control.
D-3 Newtonian/classical fit = PASS.
D-4 Special-relativistic fit = PASS.
D-5 General-relativistic/CTC fit = PASS with Spacetime owner boundary preserved.
D-6 Relational/emergent quantum-time fit = PASS PROVISIONALLY.
D-7 Distributed/computational fit = PASS.
D-8 Institutional/constituted fit = PASS.
D-9 Synthetic/virtual fit = PASS.
D-10 Biological/multiscale fit = PASS.
D-11 Agent-era fit = PASS.
D-12 ArchitectureQuestion != GroundingQuestion remains stable.
D-13 Fundamental/spacetime-geometric/relational/emergent/constituted grounding families all map to C1/C2 without selecting one universal grounding.
D-14 C1 does not duplicate WDF0 generic relation/process grammar.
D-15 C1 does not duplicate WDF3 criterion/bridge grammar.
D-16 C2 does not duplicate WDF5 Property Evaluation.
D-17 I1 Frame/Reference/Realization remains interface; core promotion not earned.
D-18 I2 Cross-Temporal-Structure Bridge remains interface; core promotion not earned.
D-19 PositiveMinimality(C1,C2) = SURVIVES D.
D-20 FoundationCandidateStageReadiness = EARNED.
D-21 FoundationFreezeReadiness = NOT ESTABLISHED.
D-22 Provisional FC1/FC2 candidate claim families defined; FC3 boundary firewall remains interface-like.
D-23 Claim-level falsifier families F1-F7 defined for later destructive closeout.
D-24 Naming pressure currently favors `Temporal Structure Architecture`, but canonical name remains unselected.
D-25 NoFoundationControl = REJECTED after positive reconstruction.
D-26 ProjectSplit = NOT EARNED.
D-27 CanonicalTemporalProjectName = NOT SELECTED.
D-28 NextWDFNumber = UNKNOWN / NOT ASSIGNED.
D-29 FirstTemporalFoundation = UNKNOWN / NOT ADMITTED.
D-30 NumberedTemporalFoundationCount = 0.
D-31 WholeWorldClosure = NOT ESTABLISHED.
D-32 Production = UNCHANGED.
```

---

# 31. Exact next round

Admit:

# **Fresh Temporal Reality Whole-Owner Search — Round E**

Subtitle:

```text
Foundation-Candidate Destructive Closeout / Naming Tournament / Admission Test
```

E must **not** assume a Foundation will be admitted.

Mandatory tasks:

```text
E1 Attack FC1/C1 using relation-only, spacetime-geometric and eliminativist controls.
E2 Attack FC2/C2 using order+measure reconstruction and metric-eliminativist controls.
E3 Search exact third-core counterexamples across all regimes.
E4 Re-run I1/I2 core-promotion attacks.
E5 Attack architecture-vs-grounding separation for circularity.
E6 Run naming tournament N1-N5 plus fresh names; do not inherit `Temporal Structure Architecture` automatically.
E7 Decide whether temporal owner line earns a canonical project name.
E8 Decide whether a numbered WDF/project identifier is warranted; do not assume WDF6.
E9 Decide whether any Foundation v1 can be admitted/frozen or only a Foundation candidate retained.
E10 Define exact FoundationReopenCondition if and only if a Foundation is frozen.
E11 Re-run no-Foundation and project-split controls.
E12 Preserve WholeWorldClosure=NOT ESTABLISHED regardless of local outcome.
```

---

# 32. Canonical frontier after D

```text
TemporalRouteAdmission = ESTABLISHED
Round A = COMPLETE
Round B = COMPLETE
Round C = COMPLETE
Round D = COMPLETE

ProvisionalTemporalArchitecture:
  C1 Temporal Relational–Topological Structure
  C2 Temporal Interval / Metric Structure

TypedInterfaces:
  I1 Frame / Reference / Realization
  I2 Cross-Temporal-Structure Bridge
  I3 Physical Spacetime realization/interface

Derived/GroundingLayers:
  D1 Orientation / Arrow Grounding
  D2 Passage / Presentness
  D3 History / Persistence / Lifetime
  D4 Frequency / Phase / Rate
  D5 Fundamental / Spacetime-Geometric / Relational /
     Emergent / Constituted grounding families

PositiveMinimality(C1,C2) = SURVIVES D
FoundationCandidateStageReadiness = EARNED
FoundationFreezeReadiness = NOT ESTABLISHED
NoFoundationControl = REJECTED
ProjectSplit = NOT EARNED
Round E = ADMITTED

CanonicalTemporalProjectName = NOT SELECTED
NextWDFNumber = UNKNOWN / NOT ASSIGNED
FirstTemporalFoundation = UNKNOWN / NOT ADMITTED
NumberedTemporalFoundationCount = 0
WholeWorldClosure = NOT ESTABLISHED
Production = UNCHANGED
```
