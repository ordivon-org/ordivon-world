# Fresh Temporal Reality Whole-Owner Search — Round A

## Current-Use / Term-Separation / Hidden-Assumption / Owner-Boundary Reconstruction

Date: 2026-08-18

Status: **A COMPLETE / TEMPORAL OWNER-LINE COHERENCE PROVISIONALLY SURVIVES / NO CANONICAL PROJECT NAME / NO WDF NUMBER / NO FOUNDATION ADMITTED**

Canonical upstream admission:

```text
NextWorldRoute
= ADMITTED
= Temporal Reality / Time–Temporal Structure fresh research

CanonicalTemporalProjectName
= NOT SELECTED

NextWDFNumber
= UNKNOWN / NOT ASSIGNED

FirstTemporalFoundation
= UNKNOWN / NOT ADMITTED

NumberedTemporalFoundationCount
= 0
```

Round A does not ask what Time *really is* in one metaphysical theory.

It first asks:

> Which distinct truth roles are currently compressed into the word `time`, which of them are genuinely temporal, which belong to neighboring owners, and what hidden assumptions make ordinary temporal language look more unified than the cross-regime evidence permits?

---

# 1. Method

A uses:

```text
1. repo-wide current-use audit across WDF0–WDF5;
2. current scientific/technical use across physics, metrology, computation,
   institutions, biology, synthetic systems and Agents;
3. matched counterexamples;
4. owner-boundary tests against WDF0/WDF1/WDF3/WDF4/WDF5;
5. rival-realization pressure from relativity, relational/emergent time,
   thermodynamics, closed-timelike structures and distributed systems;
6. anti-foundation discipline: no term is promoted merely because it is common.
```

External work is used as falsifier pressure, not authority.

---

# 2. First major result — `time` is a severe current-use overload

Current usage compresses at least the following distinct roles:

```text
TO  Temporal Order / Precedence
TL  Temporal Localization / Position / Epoch
TM  Temporal Metric / Duration / Interval
TR  Temporal Reference / Clock / Time Scale / Coordinate / Frame
TT  Temporal Topology / Global Shape
TA  Temporal Orientation / Arrow / Asymmetry
TE  Temporal Extent / Lifetime / Age / Validity Interval
TP  Passage / Presentness / Becoming / Flow claims
TF  Frequency / Period / Phase / Rate-relative temporal structure
TH  History / Trajectory / Temporal realization
```

These labels are research handles only.

They are not proposed Foundation primitives.

Canonical:

```text
TimeWord
!= OneAlreadyEstablishedWorldPrimitive
```

---

# 3. TO — Temporal Order / Precedence

Typical forms:

```text
earlier than
later than
before
after
precedes
follows
successor/predecessor
overlap
coincident / simultaneous under a criterion
```

TO answers:

```text
How are occurrences/locations ordered or partially ordered temporally?
```

It does not by itself answer:

```text
how long apart?
what clock value?
what direction is thermodynamically preferred?
what causes what?
what persists?
```

First firewall:

```text
TemporalOrder
!= Duration
!= ClockValue
!= CausalOrder
!= ThermodynamicArrow
```

---

# 4. TL — Temporal Localization / Position / Epoch

Typical forms:

```text
at t
at 2026-08-18T17:00+08:00
at proper time τ
at phase point φ
at epoch E
at system tick n
at transaction/event time t_e
```

TL answers:

```text
Where/when is an occurrence located relative to a declared temporal frame/reference?
```

Localization may require a reference system while temporal ordering may survive without a unique scalar coordinate.

Canonical:

```text
TemporalLocalization
!= TemporalOrder by identity
```

A partial order can exist without one globally valid scalar epoch assignment.

---

# 5. TM — Temporal Metric / Duration / Interval

Typical forms:

```text
elapsed time
duration
proper-time interval
latency
period
half-life
lifetime
age
inter-event interval
```

TM adds quantitative/metric structure beyond bare ordering.

Canonical:

```text
EarlierThan(a,b)
does not entail
Duration(a,b) is defined as one globally invariant scalar.
```

and:

```text
TemporalOrder != TemporalMetric
```

A regime can support precedence without a rich additive metric.

---

# 6. TR — Temporal Reference / Clock / Time Scale / Coordinate / Frame

Typical forms:

```text
clock reading
atomic clock
UTC / TAI / TT
time zone
coordinate time
proper-time clock
logical clock
simulation clock
calendar
scheduler tick
```

TR is especially overloaded because practical language says that clocks `give time`.

Round A rejects the identity:

```text
ClockReading = Time
```

A clock/reference system is a realization/evaluation/reference mechanism for temporal claims under a theory and validity envelope.

WDF5 owns much of the **evaluation/reference** machinery.

The temporal route owns the target temporal relation/quantity/status being realized or represented.

Canonical owner firewall:

```text
Clock / TimeScale / CoordinateSystem
!= TemporalReality by identity
```

and:

```text
TemporalReference
!= TemporalOrder
!= TemporalMetric
```

---

# 7. TT — Temporal Topology / Global Shape

Candidate structural possibilities include:

```text
linear order
partial order
branching structure
cyclic structure
closed-timelike structure
bounded interval
unbounded extension
dense/discrete ordering
local order without one global order
```

Round A does not claim these all describe physically realized time.

They are destructive possibilities against the hidden assumption:

```text
Time = one globally linear real-number axis
```

Canonical:

```text
TemporalTopology
!= TemporalMetric
```

A topology/order type can differ while local interval measurement remains meaningful.

---

# 8. TA — Temporal Orientation / Arrow / Asymmetry

Current usage speaks of:

```text
arrow of time
future-directed
past-directed
irreversibility
record/memory arrow
radiation arrow
decoherence arrow
causal arrow
thermodynamic arrow
```

These do not have one established identity.

Inherited WDF4 firewall:

```text
CausalDirection != TimeDirection
```

Round A adds/retains:

```text
TemporalOrder != TemporalOrientation
TemporalOrientation != ThermodynamicArrow by identity
ThermodynamicArrow != CausalArrow by identity
```

A temporal structure can be ordered without yet specifying which asymmetries are fundamental, emergent, boundary-conditioned or perspectival.

---

# 9. TE — Temporal Extent / Lifetime / Age / Validity Interval

Examples:

```text
process lifetime
organism age
contract term
credential validity interval
software support window
data freshness window
memory retention period
```

TE usually combines:

```text
boundary/identity criterion
+
temporal localization/order
+
possibly duration metric
```

Therefore:

```text
Lifetime
!= Time primitive
```

and:

```text
ValidityInterval
!= TemporalReality by identity
```

Institutional validity may be constituted by rules while still using temporal relations supplied by the temporal owner.

---

# 10. TP — Passage / Presentness / Becoming / Flow

Ordinary language often assumes:

```text
time passes
the present moves
the future becomes actual
```

These are not neutral restatements of temporal order or metric.

They belong to a contested metaphysical family.

Round A therefore separates:

```text
TemporalStructure
!= Passage/Becoming by identity
```

and:

```text
Presentness
!= TemporalLocalization under an arbitrary coordinate by identity
```

No A-theoretic, B-theoretic, presentist, eternalist or growing-block ontology is selected.

---

# 11. TF — Frequency / Period / Phase / Rate

Examples:

```text
Hz
oscillation period
circadian phase
CPU frequency
heartbeat rate
orbital phase
clock transition frequency
```

These require repeated/cyclic/process structure plus temporal comparison.

Canonical:

```text
Frequency != Time
Phase != Time
Rate != TemporalOrder
```

A frequency can help realize a time unit without becoming the ontology of temporal reality.

---

# 12. TH — History / Trajectory / Temporal Realization

Repo audit shows `history` is pervasive across WDF0–WDF5.

But upstream work already established that history can matter to identity, causation, counterfactuals, systems and property evaluation without being one root primitive.

Round A separates:

```text
History
!= Time by identity

Trajectory
!= TemporalStructure by identity
```

A history/trajectory is content organized across temporal structure.

The temporal route supplies relations/structure in which history is ordered; WDF3/WDF4/domain theory owns many questions about what persists, changes or causes what across that history.

---

# 13. Current-use hidden assumption H1 — one universal total order

Ordinary language often assumes every pair of events has one objective answer to:

```text
which happened first?
```

This is unsafe.

Relativistic spacelike separation and distributed-system concurrency both provide strong anti-total-order pressure, though their semantics are not identical.

Lamport's distributed-system construction is especially useful as a non-physical matched case:

```text
happens-before
= partial order
```

while a logical timestamp scheme can impose a total order consistent with that partial order for algorithmic purposes.

Therefore:

```text
UsefulTotalTimestampOrder
!= UnderlyingTemporal/CausalTotalOrder
```

Canonical hidden assumption:

```text
UniversalTotalTemporalOrder = REJECTED AS DEFAULT
```

---

# 14. H2 — global simultaneity

Classical discourse often treats simultaneity as an observer-independent global equivalence relation.

Relativity falsifies this as a universal physical assumption.

Round A therefore requires:

```text
Simultaneity
= criterion/frame/theory-relative unless independently established
```

and:

```text
LocalTemporalRelation
!= OneGlobalSimultaneityStructure by default
```

This does not prove that no non-relativistic or constituted regime can have a global simultaneity convention.

---

# 15. H3 — one globally valid scalar time coordinate

A scalar `t` is extraordinarily useful in models.

But:

```text
ModelParameter t
!= TemporalReality by identity
```

Different coordinates can represent the same physical situation, and some regimes lack one globally adequate scalar temporal coordinate.

Canonical:

```text
CoordinateTime
!= PhysicalTemporalRelation by default
```

---

# 16. H4 — order implies metric

False as a default.

An order/precedence structure can be meaningful before a duration metric is specified.

Distributed logical order, causal-order constructions and abstract temporal logics all pressure the collapse.

```text
TemporalOrder
!= TemporalMetric
```

remains one of Round A's strongest candidate firewalls.

---

# 17. H5 — metric implies one clock

Even where duration is defined, no single material clock or time scale is the ontology of the metric.

BIPM's SI second is defined using a fixed caesium transition frequency; international time scales are realizations/coordinate constructions requiring physical clock ensembles and relativistic corrections.

The historical replacement of solar-day-based seconds by atomic definitions also shows that changing the realization/reference standard need not mean Reality's temporal relations changed.

Therefore:

```text
UnitDefinitionRevision
!= TemporalOntologyRevision
```

and:

```text
ClockStandard
!= Time itself
```

---

# 18. H6 — duration is universally additive/path-independent

Unsafe as a generic World assumption.

In relativistic settings proper time can depend on worldline/path.

In other regimes, elapsed operational time can exclude pauses, suspension or unavailable intervals by convention.

Therefore Round A records:

```text
UniversalPathIndependentDuration
= NOT ASSUMED
```

and requires explicit metric/criterion/CVE.

---

# 19. H7 — temporal order = causal order

Rejected by WDF4 and reinforced here.

```text
Earlier(a,b)
```

is insufficient for:

```text
Cause(a,b)
```

and some causal formalisms use structures richer or differently oriented than bare temporal precedence.

Canonical:

```text
TemporalOrder != CausalOrder
```

---

# 20. H8 — temporal order = observation order

Agents and distributed systems make this failure obvious.

An event can occur before another while being observed, logged or delivered later.

Thus:

```text
EventOrder
!= ObservationOrder
!= DeliveryOrder
```

and:

```text
StaleObservation
!= PastReality by identity
```

Currentness is an epistemic/evidence relation layered over temporal claims.

---

# 21. H9 — temporal order = commit/serialization order

Databases and distributed systems can impose a commit/serialization order for correctness while messages/events have only partial causal order or different physical occurrence order.

Therefore:

```text
CommitOrder
!= EventOrder by identity

SerializationOrder
!= PhysicalTemporalOrder by identity
```

These are constituted/algorithmic temporal-ordering schemes with declared semantics.

---

# 22. H10 — time = change

A changing system requires comparison among differing states/occurrences, but the identity:

```text
Time = Change
```

is too strong.

A model can represent stationary persistence over a temporal interval.

Conversely an abstract change relation can be specified without a rich metric time structure.

Canonical:

```text
Change
!= TemporalStructure by identity
```

---

# 23. H11 — time = dynamics

Rejected from post-WDF5 coverage work.

```text
Dynamics
= laws/rules + states + conditions + evolution behavior
```

while temporal structure supplies ordering/interval/frame roles consumed by many dynamical theories.

Two systems can share temporal structure and have radically different dynamics.

Canonical:

```text
TemporalStructure != DynamicalStructure
```

---

# 24. H12 — time arrow = entropy arrow

Thermodynamic research provides strong evidence that macroscopic arrows can emerge under coarse-graining/open-system/boundary conditions even when underlying theories are time-reversal symmetric.

Therefore:

```text
ThermodynamicArrow
!= TemporalStructure by identity
```

and:

```text
EntropyIncreaseDirection
!= DefinitionOfFuture by default
```

The relation remains a dedicated stress problem for later rounds.

---

# 25. H13 — time must be fundamental/external

Relational and emergent-time programs directly pressure this assumption.

Hamiltonian GR work constructs a notion of time from configuration-space geometry; relational-clock work constructs dynamics relative to physical clocks and allows distinct clock choices.

Round A therefore rejects as default:

```text
FundamentalExternalTime = REQUIRED
```

while also refusing the opposite universal claim:

```text
TimeIsAlwaysEmergent = REQUIRED
```

Canonical:

```text
Fundamental / Relational / Emergent
= rival grounding/realization families
```

---

# 26. H14 — every good clock induces the same relational dynamics trivially

Relational-clock research shows clock choice can matter structurally; periodic and aperiodic clocks can support different relational descriptions without contradiction.

Therefore:

```text
ClockChoice
!= MereLabelChoice by default
```

But this remains a theory-specific pressure, not a generic proof that temporal reality is observer-created.

---

# 27. H15 — time must be globally acyclic

General-relativistic solution spaces contain serious closed-timelike-curve cases.

Round A does not assert their physical actuality.

It uses them to reject the hidden grammar rule:

```text
AnyTemporalStructureMustBeGloballyAcyclic
```

as a pre-research assumption.

Canonical:

```text
GlobalAcyclicity
= THEORY/REGIME CLAIM, NOT DEFAULT FOUNDATION LAW
```

---

# 28. H16 — time is necessarily continuous/dense

Continuum time is central in many physical theories, but discrete/event/order-based models exist in physics and computation.

Round A therefore leaves open:

```text
Continuity
Density
Discreteness
Granularity
```

as structural properties rather than universal assumptions.

---

# 29. H17 — time is necessarily one-dimensional

Most ordinary and physical theories use one time dimension locally/effectively.

But Round A does not promote this empirical/formal regularity into an unanalyzed World primitive.

```text
TemporalDimensionality
= OPEN / THEORY-RELATIVE BURDEN
```

Multi-time formalisms and exotic models remain falsifier pressure rather than evidence for realized extra time dimensions.

---

# 30. H18 — `now` is just a timestamp

A coordinate timestamp can identify a reference position.

But metaphysical presentness/passage claims assert more than representation membership.

Therefore:

```text
Presentness
!= TimestampEquality by identity
```

TP remains an explicit metaphysical rival burden.

---

# 31. Owner-boundary reconstruction — WDF3 identity/lifecycle

WDF3 owns:

```text
what counts as the same entity/status across transformations/history
criterion-relative persistence
lifecycle/status transitions
category/version identity
CVE
```

Temporal research owns the temporal relations/structure in which such claims are situated.

Thus:

```text
Persistence_K(x,t1,t2)
```

may consume temporal indices/order without making Time responsible for `same x`.

Canonical:

```text
Persistence != Time
IdentityAcrossTime != DefinitionOfTime
```

No WDF3 claim is falsified.

---

# 32. Owner-boundary reconstruction — WDF4 causation

WDF4 owns:

```text
causal effect
actual cause
mechanism/path
intervention/control
causal direction
```

Temporal research owns temporal ordering/orientation structures used by causal theories.

Canonical:

```text
TemporalPrecedence
!= CausalInfluence

TemporalArrow
!= CausalDirection by identity
```

A causal theory may impose temporal compatibility conditions without reducing causation to Time.

No WDF4 reopen condition fires.

---

# 33. Owner-boundary reconstruction — WDF5 Property Evaluation

WDF5 owns:

```text
clock-reading attribution validity
unit/reference semantics
cross-clock comparability
traceability
uncertainty/quality
calibration
```

Temporal research owns:

```text
the temporal target relation/quantity/status that those evaluations claim to realize
```

Therefore:

```text
ClockAccuracy != TemporalTruth by identity
ClockSynchronization != TemporalOntology
TimeScaleDefinition != TimeOntology
```

This boundary is unusually clean after WDF5 freeze.

---

# 34. Owner-boundary reconstruction — WDF1 modality/law/chance

WDF1 owns typed modal structure and leaves Law/Chance open.

Temporal claims can be indexed by:

```text
time
history
setup
frame
```

but:

```text
TimeIndex != Modality
ChanceAtTime != Time
LawTimeDependence != TimeOntology
```

Temporal research must preserve Lawhood and Chance as neighboring future routes, not absorb them.

---

# 35. Owner-boundary reconstruction — WDF0 process/change/history

WDF0 established that occurrence/process/change/history are indispensable Reality-facing families, while segmentation and identity are criterion/scale sensitive.

Round A therefore does not create:

```text
Time = Process
```

or:

```text
Time = History
```

Instead:

```text
Processes/occurrences have temporal realization/relations;
histories organize content across temporal structure.
```

The temporal route provides structural resources consumed by process/history models.

---

# 36. Owner-boundary reconstruction — Spacetime / physical geometry

Post-WDF5 R2 preserved physical Spacetime / Locality / Geometry as a future regime-depth route.

Temporal research must therefore avoid swallowing:

```text
metric tensor ontology
spatial topology
locality/nonlocality
background independence
full spacetime geometry
gravitational dynamics
```

Canonical interface:

```text
GenericTemporalStructure
!= FullPhysicalSpacetimeArchitecture
```

Relativity is a mandatory falsifier for Time, not evidence that the generic temporal owner and full spacetime owner are identical.

---

# 37. Newtonian/classical regime inventory

Classical mechanics often packages:

```text
one global parameter t
one global simultaneity structure
additive duration
unbounded/dense continuum
```

This regime is useful but dangerously intuitive.

Round A records it as:

```text
strong local/current-use template
!= universal temporal foundation
```

The major hidden assumptions H1–H6 largely arise from importing this template universally.

---

# 38. Special/general relativity inventory

Relativistic regimes separate:

```text
coordinate time
proper time
worldline
frame/reference system
spacelike/timelike/null separation
local vs global simultaneity
causal/chronological structure
```

Important Round A pressure:

```text
one global temporal coordinate is not physically privileged by default;
proper-time-like duration is worldline-relative;
spacelike separated events do not support one invariant total before/after relation.
```

This makes relativity the strongest current falsifier of naive global scalar Time.

---

# 39. Hamiltonian / relational / emergent-time inventory

Recent and historical approaches show that temporal dynamics can sometimes be reconstructed from:

```text
configuration-space geometry
Hamiltonian constraints
relations among subsystems
physical clock variables
conditional/relational observables
```

Round A does not adopt these as truth.

It records the owner-level pressure:

```text
TemporalRoleCanBeRealizedWithoutPrimitiveExternalTime
```

under at least some serious theories.

Thus primitive-external-time cannot be the entry axiom.

---

# 40. Quantum / quantum-gravity inventory

The `problem of time` arises because ordinary quantum dynamics and generally covariant gravitational structure do not fit together trivially under one external time parameter.

Relational-clock approaches and newer anomaly/diffeomorphism-invariant proposals continue to treat temporal structure as reconstruction-sensitive.

Round A therefore marks:

```text
QuantumTimeInterface = MAJOR OPEN STRESS
```

without concluding:

```text
TimeIsQuantum
TimeDoesNotExist
TimeIsAlwaysEmergent
```

---

# 41. Thermodynamic / irreversibility inventory

Thermodynamics supplies:

```text
irreversibility
entropy-production orientation
relaxation
coarse-graining
boundary/history asymmetry
fluctuations
open-system exchange
```

These create powerful temporal asymmetries.

But Round A treats them as:

```text
Arrow/Orientation realizations or correlates
```

not definitions of basic temporal order/metric.

---

# 42. Biological / evolutionary temporal inventory

Biology uses temporal notions including:

```text
age
life cycle
developmental stage
generation
circadian phase
seasonality
evolutionary history
reaction time
biological rate
```

These combine distinct roles:

```text
age -> extent/duration + identity criterion
stage -> constituted/category status + temporal/process relation
circadian phase -> cyclic phase relative to oscillator/reference
lineage/history -> WDF3 identity/genealogy + temporal order
rate -> process change per temporal metric
```

Canonical:

```text
BiologicalTime
!= one new biological temporal substance by default
```

Biology is instead a high-value multi-scale/effective temporal stress regime.

---

# 43. Institutional temporal inventory

Institutions use:

```text
effective date
expiry
term
filing deadline
business day
calendar period
fiscal year
market session
legal age
retrospective/prospective effect
```

These are partly constituted.

A contract can become valid at a rule-defined time even though the underlying physical chronology is unchanged.

Thus:

```text
InstitutionalTemporalStatus
= constituted temporal-use structure
```

and:

```text
InstitutionalEffectiveTime
!= PhysicalTemporalOntology by identity
```

WDF3 constitution/category and the temporal route must compose.

---

# 44. Computational / distributed temporal inventory

Computing distinguishes, among others:

```text
wall-clock time
monotonic clock
logical clock
process-local time
event time
ingestion time
processing time
commit time
transaction time
lease/TTL time
scheduler tick
simulation time
causal/happens-before order
serialization order
```

Lamport's construction is decisive current-use pressure:

```text
partial happens-before relation
!= arbitrary/useful total logical-clock order
```

Thus software exposes temporal **plurality of roles** exceptionally clearly.

No software-specific clock primitive should be imported into World ontology.

---

# 45. Synthetic / virtual-world temporal inventory

Synthetic worlds can stipulate:

```text
paused time
accelerated/slowed simulation time
turn-based time
multiple local clocks
rewind/reset
branching saves
cyclic episodes
scripted simultaneity
```

These constituted structures are real within their declared synthetic regime while their relation to host physical time is separate.

Canonical:

```text
SyntheticTemporalStructure
!= HostPhysicalTime by identity
```

and:

```text
TimeScaleTransportAcrossWorlds
requires an explicit bridge.
```

This parallels WDF5 transport discipline without reducing Time to evaluation.

---

# 46. Agent-era temporal inventory

Agent systems amplify:

```text
asynchronous sensing
stale observations
concurrent tool actions
future commitments
lease/authorization windows
checkpoint/recovery histories
policy-effective intervals
multiple execution clocks
replay/simulation branches
prediction horizons
planning horizons
```

A single Agent turn may involve:

```text
Reality event time
provider processing time
Host commit time
Agent observation time
message delivery time
user-visible time
```

These can diverge.

Canonical:

```text
EventTime
!= ObservationTime
!= ProcessingTime
!= CommitTime
!= DeliveryTime
```

This is high-information pressure for role typing, not evidence that Agent systems create a fundamentally new Time ontology.

---

# 47. Matched case A1 — same order, different metric

Construct two temporal structures with identical precedence relation:

```text
a < b < c
```

but different intervals:

```text
Model 1: d(a,b)=1, d(b,c)=1
Model 2: d(a,b)=1, d(b,c)=100
```

Ordering is preserved while metric claims differ.

Therefore:

```text
TemporalOrder != TemporalMetric
```

---

# 48. Matched case A2 — same clock labels, different target semantics

Two systems both emit:

```text
12:00:00
```

but one label denotes UTC and another a simulation-local clock with arbitrary epoch/rate.

Same representation does not imply same temporal target semantics.

Therefore:

```text
SameClockLabel != SameTemporalLocalization
```

This composes directly with WDF5 PS/VS/RE discipline.

---

# 49. Matched case A3 — same event set, different useful orders

Distributed events can admit:

```text
happens-before partial order H
```

while an algorithm chooses one total serialization order S extending H.

Then:

```text
H != S
```

though S may be operationally valid for mutual exclusion/state-machine replication.

Therefore:

```text
OperationalTotalOrder
!= UnderlyingPartialOrder by identity
```

---

# 50. Matched case A4 — same temporal order, opposite/absent thermodynamic asymmetry

A microscopic time-reversal-symmetric model can use the same temporal parameter/order while macroscopic entropy-increase behavior depends on boundary/coarse-graining conditions.

Therefore:

```text
TemporalOrder
!= ThermodynamicArrow
```

---

# 51. Matched case A5 — stationary persistence

A system remains in the same declared macrostate over interval I.

Temporal interval/ordering exists in the model while the target macrostate does not change.

Therefore:

```text
Time = Change
```

fails as an identity criterion.

---

# 52. Matched case A6 — same physical history, different institutional effective time

Two legal regimes can assign different effective dates to otherwise identical physical document-signing histories.

The institutional status changes while the underlying physical event chronology is held fixed.

Therefore:

```text
InstitutionalEffectiveTime
!= PhysicalEventOrder by identity
```

but both can share temporal relations through an explicit constitution/reference bridge.

---

# 53. Matched case A7 — periodic vs aperiodic relational clock

A system may be described relative to a periodic clock and relative to an aperiodic/monotonic clock.

Recent relational-clock work shows the resulting relational dynamics can differ in form without inconsistency.

Therefore:

```text
ClockChoice
!= PureNotation by default
```

while:

```text
ClockChoiceDifference
!= ProofOfSubjectiveTime
```

---

# 54. Matched case A8 — clock standard revision without temporal-world revision

Historical time metrology moved from astronomical/solar references toward atomic frequency definitions, and current BIPM work is considering future optical-clock-based redefinition of the second.

This is powerful evidence for:

```text
ReferenceStandardRevision
!= TemporalRealityRevision by identity
```

The evaluated unit/reference can improve while the target temporal relations are not thereby recreated.

---

# 55. Matched case A9 — closed-timelike-curve falsifier

A GR solution containing a closed timelike curve violates simple global acyclic/linear temporal intuitions while retaining local timelike structure along worldlines.

Round A uses this only as a consistency/falsifier case.

Therefore:

```text
LocalTemporalStructure
!= GlobalAcyclicTemporalTopology by identity
```

---

# 56. Matched case A10 — partial order without unique global metric

A distributed/causal ordering can establish:

```text
a precedes b
```

without establishing a unique physically meaningful scalar duration between every pair of incomparable events.

Again:

```text
Order != Metric
```

survives outside one domain.

---

# 57. Strong term-separation ledger after A

```text
TemporalOrder
!= TemporalLocalization
!= TemporalMetric
!= TemporalReference
!= TemporalTopology
!= TemporalOrientation
!= Passage/Presentness
```

Additional separations:

```text
Time != Change
Time != Dynamics
Time != Causation
Time != ThermodynamicIrreversibility
Time != ClockReading
Time != CoordinateParameter
Time != History
Time != Persistence

Clock != TimeScale != Unit != Coordinate != TemporalTarget

Earlier/Later != Duration
Simultaneity != SameClockLabel
CausalOrder != ObservationOrder != CommitOrder != SerializationOrder
Frequency != Duration != Phase
Age != Identity
ValidityInterval != PhysicalChronology by identity
```

These are Round A research firewalls, not frozen Foundation laws.

---

# 58. Does the temporal owner line survive term separation?

Yes, provisionally.

Despite severe lexical overload, TO/TL/TM/TT/TA repeatedly cluster around a reusable burden:

```text
temporal relations / structures
that order, locate, separate, orient or topologically organize
occurrences/states/processes
```

The following are strongly connected but partly neighboring/derived:

```text
TR Clock/reference
-> realization/evaluation/interface-heavy; substantial WDF5 ownership

TE Lifetime/age/validity interval
-> temporal structure + WDF3/domain boundary/identity/constitution

TH History/trajectory
-> content/process across temporal structure

TP Passage/presentness
-> additional metaphysical claim family, not required by all temporal theories

TF Frequency/phase/rate
-> process/cycle + temporal metric/reference structure
```

Therefore:

```text
TemporalOwnerLineCoherence
= PROVISIONALLY SURVIVES A
```

but:

```text
OnePrimitiveCalledTime
= NOT ESTABLISHED
```

---

# 59. Project-split control

Possible premature split:

```text
Project 1 temporal order
Project 2 duration/metric
Project 3 clocks/time scales
Project 4 arrows
Project 5 metaphysics of passage
```

Round A rejects immediate split.

Why:

```text
1. Real cases repeatedly require typed composition among order, metric,
   reference and topology.
2. Relativity jointly constrains ordering, metric, frame and clock semantics.
3. Distributed systems jointly expose order/reference/serialization distinctions.
4. Thermodynamics jointly pressures order/orientation/history.
5. A split before rival-architecture testing would hide rather than resolve
   whether these roles share one thin temporal substrate.
```

Canonical:

```text
ProjectSplit
= NOT EARNED IN A
```

Passage/presentness remains a candidate detachable metaphysical layer to test later.

---

# 60. Naming pressure after A

The historical route label:

```text
Temporal Reality / Time–Temporal Structure
```

remains a neutral handoff label.

Round A finds that `Time` is lexically overloaded, while `Temporal Structure` better tracks much of the shared research burden.

However:

```text
CanonicalProjectName
= NOT SELECTED
```

because later rival testing could show that:

```text
order-first
metric-first
relation-first
spacetime-geometric
relational/emergent
passage-centered
plural typed
```

architectures carve the owner differently.

No rename is admitted in A.

---

# 61. Event/occurrence primitive control

Many temporal theories are phrased over `events`.

Round A does not therefore promote:

```text
Event = TemporalPrimitive
```

WDF0/WDF3 already establish that occurrence/event segmentation can be criterion/scale dependent.

Temporal structure may relate:

```text
events
states
process stages
boundaries
worldline points
configurations
institutional status transitions
computational actions
```

under different theories.

Canonical:

```text
TemporalRelata
= TYPED / THEORY-RELATIVE
```

until a later round earns a narrower claim.

---

# 62. Provisional temporal-claim grammar

For research only, A introduces a non-canonical grammar:

```text
TemporalClaim_R(x,y | F,K,CVE)
```

where:

```text
R
= declared temporal relation/quantity
  e.g. earlier-than, overlap, duration, same-epoch-under-frame,
  phase, orientation, interval membership

F
= temporal frame/reference/structural context where relevant

K
= target/relata criterion inherited from WDF3 where needed

CVE
= validity envelope
```

This is not a Foundation schema.

Its purpose is only to stop surface claims such as:

```text
x happened at time 12
```

from hiding whether `12` denotes:

```text
coordinate
clock reading
logical timestamp
calendar epoch
simulation tick
phase
```

and what temporal relation is actually asserted.

---

# 63. Candidate rival architectures generated by A

Round A does not rank these yet.

## RA1 — Order-first / relation-first temporal architecture

Core intuition:

```text
temporal precedence/order is primary;
metric, clocks and orientation enrich it.
```

Strength:

```text
works naturally with partial orders, distributed systems,
causal-set-like structures and nonmetric temporal logics.
```

Pressure:

```text
can metric/duration be reconstructed without hidden import?
what about simultaneity/equivalence classes, proper time and cyclic clocks?
```

## RA2 — Metric/duration-first architecture

Core intuition:

```text
temporal interval/duration structure is primary;
order can be recovered from oriented metric/geometric structure.
```

Strength:

```text
fits physical measurement and spacetime geometry well.
```

Pressure:

```text
order may exist without full metric;
metric may be path/frame dependent;
requires orientation/topology assumptions.
```

## RA3 — Clock/reference operational architecture

Core intuition:

```text
time is what valid clocks/time-reference systems realize/measure.
```

Strength:

```text
excellent operational connection to science/metrology.
```

Pressure:

```text
ClockReading != TemporalReality;
reference standards can change;
relational clock choice can matter;
WDF5 ownership overlap is severe.
```

## RA4 — Change/process relational architecture

Core intuition:

```text
time is abstracted from change/order among processes.
```

Strength:

```text
avoids external container time;
fits relationist/process traditions.
```

Pressure:

```text
stationary persistence cases;
change relation may presuppose ordering;
metric and topology reconstruction burden remains.
```

## RA5 — Spacetime-geometric architecture

Core intuition:

```text
temporal structure is one aspect of spacetime geometry/causal structure.
```

Strength:

```text
strong in relativity and modern fundamental physics.
```

Pressure:

```text
may overfit physical spacetime;
computational/institutional/synthetic temporal structures are not
full Lorentzian geometries;
spacetime route has separate owner depth.
```

## RA6 — Relational/emergent temporal architecture

Core intuition:

```text
temporal roles emerge from configuration/change/correlation/clock relations
rather than primitive external time.
```

Strength:

```text
serious Hamiltonian and quantum-gravity motivation.
```

Pressure:

```text
emergent from what structure?
can emergence avoid covert temporal ordering?
are all regimes relational/emergent?
```

## RA7 — Passage/presentness-first architecture

Core intuition:

```text
becoming/presentness is constitutive of Time.
```

Strength:

```text
captures ordinary temporal experience and A-theoretic metaphysics.
```

Pressure:

```text
relativistic compatibility;
formal necessity;
can order/metric exist without passage?
experience != Reality truth.
```

## RA8 — Typed plural thin-core architecture

Core intuition:

```text
there is no one reductive temporal primitive;
a small typed family of order, metric, reference, topology and orientation
may be irreducible but compositional.
```

Strength:

```text
fits Round A separations.
```

Pressure:

```text
may merely catalogue distinctions rather than explain them;
possible hidden compression into one deeper relation/geometry.
```

## RA9 — Eliminativist / domain-local control

Core intuition:

```text
there is no generic World temporal architecture;
physics, computation, institutions and biology each define local time-like structures.
```

Strength:

```text
avoids false unification.
```

Pressure:

```text
cross-regime recurrence of order/duration/reference distinctions may force
reconstruction of the same generic grammar repeatedly.
```

---

# 64. Round A residual burden ranking

After term separation, the highest unresolved burdens are:

```text
A-R1
Can temporal order/precedence serve as a thin common root,
or is it already theory-dependent / derivative?

A-R2
What is the relation between order and metric/duration?

A-R3
What temporal role is invariant across frame/clock/reference changes?

A-R4
How much topology/global structure is required beyond local relations?

A-R5
What, if anything, grounds temporal orientation/arrows?

A-R6
Can relational/emergent accounts reconstruct temporal roles
without circularly presupposing them?

A-R7
Is passage/presentness owner-native or an additional metaphysical overlay?

A-R8
Does a generic temporal owner survive fundamental physics +
distributed/institutional/synthetic counterexamples,
or should the line split/eliminate?
```

Highest immediate information gain:

```text
A-R1 + A-R2 + A-R6 + A-R8
```

These require rival-architecture destructive comparison rather than more vocabulary inventory.

---

# 65. Exact next round

Admit:

# **Fresh Temporal Reality Whole-Owner Search — Round B**

Subtitle:

```text
Rival Thin Temporal Architectures / Minimal Truth-Burden / Destructive Tournament
```

B must compare at least RA1–RA9.

Mandatory matched attacks:

```text
B1 same order / different metric
B2 same metric representation / different frame semantics
B3 partial order with no global total order
B4 stationary persistence
B5 time-reversal-symmetric law + thermodynamic arrow
B6 periodic vs aperiodic relational clocks
B7 relativistic proper time / coordinate time distinction
B8 CTC/global acyclicity falsifier
B9 distributed logical timestamp vs happens-before
B10 institutional effective date vs physical chronology
B11 simulation pause/rewind/branch vs host physical time
B12 emergent-time circularity test
B13 passage/presentness deletion test
B14 no-generic-temporal-owner control
```

Round B may reconstruct a provisional temporal core.

It may **not**:

```text
select canonical project name automatically
assign WDF number
create a Foundation
freeze Time ontology
```

---

# 66. External pressure used in A

External work is used as falsification/context only.

## BIPM / time metrology

Current SI defines the second through a fixed caesium-133 transition frequency; TAI is a constructed continuous time scale based on realizations of the SI second, with relativistic conversion from local clock proper time where relevant. BIPM is also actively considering future optical-frequency redefinition paths.

Pressure:

```text
Unit/reference realization != Temporal ontology
Clock standard can change without rewriting temporal Reality.
```

## Lamport 1978

`Time, Clocks, and the Ordering of Events in a Distributed System` separates a partial happens-before relation from logical clocks/total orders used to implement distributed algorithms.

Pressure:

```text
UsefulTimestampOrder != underlying event/causal order by identity.
```

## Kaushal, Prabhakar & Wadia 2024

`Emergent Time in Hamiltonian General Relativity` constructs time from configuration-space geometry in Hamilton-Jacobi/Hamiltonian settings.

Pressure:

```text
Primitive external time is not mandatory in every serious theory.
```

## Chataignier, Hoehn, Lock & Mele 2024

`Relational Dynamics with Periodic Clocks` develops classical/quantum relational dynamics relative to periodic clocks and shows clock choice can affect the relational description nontrivially.

Pressure:

```text
ClockChoice != mere representation label by default.
```

## Klinger & Leigh 2025

`The Problem of Time and its Quantum Resolution` treats the problem of time as indicating a structural problem in standard quantum-gravity framing and proposes a quantum diffeomorphism-invariant temporal notion.

Pressure:

```text
Generic Time research must tolerate reconstruction-sensitive temporal roles.
```

## Vidotto 2024

`Thermodynamics without Time` reviews relational/covariant thermodynamic constructions and time-arrow emergence pressure.

Pressure:

```text
ThermodynamicArrow != generic temporal structure by identity.
```

## General-relativistic CTC literature

Closed-timelike-curve solution families provide destructive pressure against assuming global acyclicity as a logical truth of every temporal model.

A does not claim CTC actuality.

---

# 67. Foundation reopen audit

```text
FoundationReopenCondition(WDF0) = NOT FIRED
FoundationReopenCondition(WDF1) = NOT FIRED
FoundationReopenCondition(WDF2) = NOT FIRED
ExactWDF3ClaimFalsified = NONE
FoundationReopenCondition(WDF4) = NOT FIRED
FoundationReopenCondition(WDF5) = NOT FIRED
```

Round A consumes rather than falsifies upstream architecture.

---

# 68. Canonical state after A

```text
TemporalRouteAdmission
= ESTABLISHED

FreshTemporalWholeOwnerSearchA
= COMPLETE

TemporalOwnerLineCoherence
= PROVISIONALLY SURVIVES

OnePrimitiveCalledTime
= NOT ESTABLISHED

ProjectSplit
= NOT EARNED IN A

CanonicalTemporalProjectName
= NOT SELECTED

NextWDFNumber
= UNKNOWN / NOT ASSIGNED

FirstTemporalFoundation
= UNKNOWN / NOT ADMITTED

NumberedTemporalFoundationCount
= 0

RoundB
= ADMITTED
= Rival Thin Temporal Architectures / Minimal Truth-Burden / Destructive Tournament

WholeWorldClosure
= NOT ESTABLISHED

Production
= UNCHANGED
```

---

# 69. Production boundary

Do not infer engineering work from A.

Do not create automatically:

```text
clock abstraction
time service
event-order schema
logical-clock library
temporal database model
scheduler rewrite
UTC/TAI conversion layer
time synchronization service
event-sourcing architecture
```

Engineering consumption remains separately admitted/audited.
