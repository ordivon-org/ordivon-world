{
  "schema_version": 1,
  "adapter_id": "network-observation",
  "adapter_revision": "0.1.0",
  "owner": "source-native-observation-module",
  "authority": [
    "method-bound DNS, HTTP, TLS, QUIC, transfer and connection-lifetime observations",
    "observation time, target, probe revision and raw result",
    "sanitized local observation history"
  ],
  "real_consumers": [
    "ordivon-host StateRef projection",
    "private network diagnosis and path-condition experiments"
  ],
  "consequence_class": "bounded observation by default; explicit private network mutation remains manual",
  "cost_model": {
    "fixed": "local Rust binaries and optional sanitized SQLite history",
    "variable": "probe traffic, latency and optional private operator attention"
  },
  "retention_source": "consumer-selected local history and evidence retention",
  "deletion_trigger": "delete any probe or retained field that does not alter a real Task decision, verification or recovery path",
  "facets": ["capability", "observation"],
  "capabilities": [
    {"id": "network.http-tls.observe", "contract": "probe-result.v1", "execution_mode": "source-native-observation", "state": "ready"},
    {"id": "network.quic.observe", "contract": "probe-result.v1", "execution_mode": "source-native-observation", "state": "ready"},
    {"id": "network.transfer.observe", "contract": "probe-result.v1", "execution_mode": "source-native-observation", "state": "ready"},
    {"id": "network.connection-lifetime.observe", "contract": "probe-result.v1", "execution_mode": "source-native-observation", "state": "ready"}
  ]
}
