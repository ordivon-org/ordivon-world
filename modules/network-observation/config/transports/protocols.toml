schema_version = 1
inspected_at = "2026-07-26"

[[transports]]
id = "wireguard-go"
family = "wireguard"
implementation = "wireguard-go"
role = "baseline"
source_url = "https://git.zx2c4.com/wireguard-go"
source_revision = "ecfc5a8d54462e18e13c72173e2623d16d8e25a0"
license = "MIT"
source_status = "open_source"
language = "Go"
layers = ["ip_tunnel"]
carriers = ["udp"]
primary_security = "NoiseIK-style authenticated key exchange and AEAD-protected IP packets."
camouflage = "None by design; the protocol prioritizes a small auditable tunnel over traffic mimicry."
strengths = [
  "Small protocol and implementation surface compared with feature-heavy VPN stacks.",
  "Kernel and userspace implementations use a stable peer, key, endpoint, and AllowedIPs model.",
  "Good baseline for separating tunnel correctness and performance from camouflage claims.",
]
limitations = [
  "UDP reachability and path quality are hard dependencies.",
  "Stable handshake and packet behavior can be classified even though payloads are encrypted.",
  "Roaming changes endpoints but does not provide an application-looking cover protocol.",
  "Full-tunnel routing, DNS, MTU, kill-switch, and leak prevention remain host integration problems.",
]
code_paths = [
  "device/noise-protocol.go",
  "device/send.go",
  "device/receive.go",
  "device/allowedips.go",
  "conn/bind_std.go",
  "tun/tun.go",
]

[[transports]]
id = "openvpn"
family = "openvpn"
implementation = "OpenVPN community daemon"
role = "baseline"
source_url = "https://github.com/OpenVPN/openvpn"
source_revision = "a6537f7549f70f16bb2efd5c0683e0e94a236f0c"
license = "GPL-2.0"
source_status = "open_source"
language = "C"
layers = ["ip_tunnel"]
carriers = ["tcp", "udp", "tls"]
primary_security = "TLS control channel plus an authenticated encrypted data channel with configurable crypto backends."
camouflage = "Can run over common ports and transports, but the OpenVPN state machine and active responses are not ordinary HTTPS."
strengths = [
  "Mature cross-platform implementation and operational ecosystem.",
  "Supports both routed TUN and bridged TAP designs and many authentication models.",
  "Useful compatibility baseline for feature completeness and operational complexity.",
]
limitations = [
  "Large configuration and compatibility surface increases deployment and audit cost.",
  "TCP-over-TCP can amplify loss and head-of-line blocking when the tunnel itself uses TCP.",
  "Protocol fingerprints and active probing can identify many deployments despite encryption.",
  "Certificate, cipher, compression, plugin, route, and platform options create configuration risk.",
]
code_paths = [
  "src/openvpn/openvpn.c",
  "src/openvpn/forward.c",
  "src/openvpn/ssl.c",
  "src/openvpn/ssl_pkt.c",
  "src/openvpn/crypto.c",
  "src/openvpn/reliable.c",
  "src/openvpn/socket.c",
  "src/openvpn/tun.c",
]

[[transports]]
id = "shadowsocks-rust"
family = "shadowsocks"
implementation = "shadowsocks-rust"
role = "comparator"
source_url = "https://github.com/shadowsocks/shadowsocks-rust"
source_revision = "6b96a84b9943a5e6b267778e98992c1ff3c5bce1"
license = "MIT"
source_status = "open_source"
language = "Rust"
layers = ["stream_proxy", "datagram_proxy"]
carriers = ["tcp", "udp"]
primary_security = "Password-derived keys and AEAD framing protect proxied TCP and UDP payloads."
camouflage = "Encrypted opaque traffic; the core protocol does not inherently reproduce a normal browser protocol."
strengths = [
  "Relatively small proxy semantics and broad client/server implementation support.",
  "Clear separation between core cipher framing and local/server relay services.",
  "Rust implementation provides a useful memory-safe code comparator for Edge adapters.",
]
limitations = [
  "Opaque high-entropy traffic can itself become a classifier feature.",
  "Server address reuse and user traffic concentration remain visible metadata.",
  "UDP association, DNS, transparent proxying, and TUN behavior add implementation-specific complexity.",
  "Legacy cipher compatibility expands the unsafe configuration surface and should remain disabled.",
]
code_paths = [
  "crates/shadowsocks/src/relay/tcprelay/aead.rs",
  "crates/shadowsocks/src/relay/tcprelay/aead_2022.rs",
  "crates/shadowsocks/src/relay/tcprelay/crypto_io.rs",
  "crates/shadowsocks/src/relay/udprelay/aead.rs",
  "crates/shadowsocks-service/src/local/net/tcp/listener.rs",
  "crates/shadowsocks-service/src/server/tcprelay.rs",
]

[[transports]]
id = "xray-vless-reality"
family = "vless-reality"
implementation = "Xray-core"
role = "candidate"
source_url = "https://github.com/XTLS/Xray-core"
source_revision = "d291486c544abe606bc3227dac4df84a1aa67845"
license = "MPL-2.0"
source_status = "open_source"
language = "Go"
layers = ["stream_proxy", "datagram_proxy"]
carriers = ["tcp", "tls", "http2", "http3", "quic"]
primary_security = "VLESS supplies proxy addressing and authentication semantics while REALITY modifies the TLS-facing authentication and handshake path."
camouflage = "Attempts to make the externally observed TLS path resemble a selected legitimate destination and to avoid confirming the proxy to unauthenticated probes."
strengths = [
  "Native reference implementation for VLESS, XTLS Vision, REALITY, XHTTP, and related transports.",
  "Separates proxy encoding from transport and TLS/REALITY layers.",
  "REALITY directly addresses unauthenticated active-probe confirmation rather than only encrypting payloads.",
]
limitations = [
  "The composition has many interacting modes, so configuration and version drift can dominate protocol theory.",
  "A fixed endpoint, traffic volume, timing, and long-lived behavior remain externally observable.",
  "Borrowing a TLS-facing appearance is not equivalent to reproducing complete browser and application behavior.",
  "MPL file-level copyleft and protocol-specific implementation claims require careful downstream license review.",
]
code_paths = [
  "proxy/vless/encoding/encoding.go",
  "proxy/vless/outbound/outbound.go",
  "proxy/vless/inbound/inbound.go",
  "transport/internet/reality/reality.go",
  "transport/internet/reality/config.go",
  "transport/internet/tls/ech.go",
]

[[transports]]
id = "hysteria2"
family = "hysteria2"
implementation = "apernet/hysteria"
role = "candidate"
source_url = "https://github.com/apernet/hysteria"
source_revision = "9aa898c5bb3a734a620391ae3d101dda36d84bfd"
license = "MIT"
source_status = "open_source"
language = "Go"
layers = ["stream_proxy", "datagram_proxy"]
carriers = ["udp", "quic", "tls", "http3"]
primary_security = "TLS-authenticated QUIC session carrying authenticated TCP streams and UDP messages."
camouflage = "HTTP/3 masquerade plus optional obfuscation; unauthenticated requests can be handled by a normal-looking HTTP service."
strengths = [
  "Designed around lossy and high-latency paths rather than treating them as exceptional.",
  "Contains explicit congestion-control, fragmentation, PMTUD, obfuscation, masquerade, and UDP-hop components.",
  "Protocol specification is committed beside the implementation.",
]
limitations = [
  "UDP or QUIC impairment can make the entire path unavailable even when TCP remains usable.",
  "Aggressive congestion behavior may improve throughput while increasing burstiness, loss, fairness, or provider risk.",
  "QUIC and HTTP/3 handshakes still expose addresses and observable protocol metadata.",
  "Masquerade and obfuscation reduce some confirmation signals but do not erase endpoint reputation or traffic analysis.",
]
code_paths = [
  "PROTOCOL.md",
  "core/internal/protocol/proxy.go",
  "core/client/client.go",
  "core/server/server.go",
  "core/internal/congestion/brutal/brutal.go",
  "core/internal/congestion/bbr/bbr_sender.go",
  "extras/obfs/salamander.go",
  "extras/masq/server.go",
  "extras/transport/udphop/conn.go",
]

[[transports]]
id = "tuic-v5-spec"
family = "tuic"
implementation = "TUIC protocol specification"
role = "reference"
source_url = "https://github.com/tuic-protocol/tuic"
source_revision = "8e118f242f24a17a9f487dc344cc50d7e63e557e"
license = "GPL-3.0 repository; protocol concept declared license-free by the project"
source_status = "specification_only"
language = "Markdown specification"
layers = ["stream_proxy", "datagram_proxy", "specification"]
carriers = ["udp", "quic", "tls"]
primary_security = "QUIC/TLS connection carrying authenticate, connect, packet, dissociate, and heartbeat commands."
camouflage = "QUIC transport; the specification does not itself guarantee browser-equivalent HTTP/3 behavior."
strengths = [
  "Compact protocol document makes command framing and flow easy to inspect.",
  "0-RTT-oriented QUIC design targets low connection setup cost and multiplexing.",
  "Multiple independent implementations make interoperability comparison possible.",
]
limitations = [
  "The specification repository states that it contains no official implementation.",
  "Error handling is intentionally under-specified, increasing implementation divergence.",
  "0-RTT use requires replay-aware application semantics and careful credential handling.",
  "QUIC dependence inherits UDP reachability, MTU, NAT, and path-quality constraints.",
]
code_paths = [
  "SPEC.md",
  "README.md",
]

[[transports]]
id = "naiveproxy"
family = "naiveproxy"
implementation = "NaiveProxy"
role = "comparator"
source_url = "https://github.com/klzgrad/naiveproxy"
source_revision = "3ba967e2d36cc133a896e81a36257ad4c6ea20f4"
license = "BSD-3-Clause"
source_status = "open_source"
language = "C++ with minimized Chromium source"
layers = ["stream_proxy"]
carriers = ["tls", "http2", "http3", "quic"]
primary_security = "Standard Chromium TLS and HTTP CONNECT semantics with proxy authentication."
camouflage = "Reuses Chromium's network stack and ordinary HTTP/2 or HTTP/3 frontends, with targeted padding and frame-shape changes."
strengths = [
  "Uses a real browser networking stack instead of manually imitating selected TLS parameters.",
  "Frontend application routing can send unauthenticated probes to ordinary content.",
  "Documents concrete padding and frame-shape countermeasures rather than only claiming generic obfuscation.",
]
limitations = [
  "Chromium-derived code and release tracking produce a large build and maintenance burden.",
  "Users must stay close to current Chrome versions for the traffic-shape argument to remain credible.",
  "HTTP CONNECT proxy behavior can still differ from ordinary browsing at session and traffic levels.",
  "The master branch is rebased across Chromium imports, so stable tags are required for reproducibility.",
]
code_paths = [
  "src/net/tools/naive/",
  "README.md",
  "CHROMIUM_VERSION",
  ".github/workflows/build.yml",
]

[[transports]]
id = "sing-box"
family = "multi-protocol-platform"
implementation = "sing-box"
role = "platform"
source_url = "https://github.com/SagerNet/sing-box"
source_revision = "f60f3728ce618d97abb59ead8d7059df984ea1a0"
license = "GPL-3.0-or-later with additional naming restriction"
source_status = "open_source"
language = "Go"
layers = ["ip_tunnel", "stream_proxy", "datagram_proxy", "multiprotocol_platform"]
carriers = ["tcp", "udp", "quic", "tls", "http2", "http3"]
primary_security = "Delegates security to protocol-specific implementations and mature TLS/QUIC libraries while providing routing and lifecycle orchestration."
camouflage = "Varies by selected outbound: VLESS, Hysteria2, TUIC, Shadowsocks, Naive, and other adapters have different observable shapes."
strengths = [
  "One runtime integrates TUN, DNS, routing rules, process-aware selection, and many replaceable outbounds.",
  "Protocol implementations are separated into narrow packages suitable for adapter-level study.",
  "Strong candidate for Ordivon Link's local data-plane orchestration without inventing a new proxy core.",
]
limitations = [
  "The broad feature matrix creates configuration drift, compatibility, and upgrade risk.",
  "A single process can become a large shared failure domain across routing, DNS, TUN, and transports.",
  "Bundled support does not mean every protocol path is equally mature or semantically identical to its native implementation.",
  "GPL-3.0-or-later affects redistribution and derivative integration choices.",
]
code_paths = [
  "box.go",
  "route/router.go",
  "protocol/tun/inbound.go",
  "protocol/vless/outbound.go",
  "protocol/hysteria2/outbound.go",
  "protocol/tuic/outbound.go",
  "protocol/shadowsocks/outbound.go",
  "protocol/naive/outbound.go",
]
